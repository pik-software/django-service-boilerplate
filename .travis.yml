language: python
os:
  - linux
  - osx

python:
  - "3.5"

install:
  - pip install virtualenv
  - pip install virtualenvwrapper
  - npm install -g bower

script:
  - mkvirtualenv project-name
  - inv init
